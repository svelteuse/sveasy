{
  "name": "sveasy",
  "version": "0.2.3",
  "homepage": "https://github.com/alexanderniebuhr/sveasy#readme",
  "author": "Alexander Niebuhr",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexanderniebuhr/sveasy.git"
  },
  "bugs": {
    "url": "https://github.com/alexanderniebuhr/sveasy/issues"
  },
  "license": "ISC",
  "bin": "bin/sveasy.mjs",
  "main": "lib/index.mjs",
  "exports": {
    "import": "./lib/index.mjs"
  },
  "files": [
    "lib/**/*",
    "bin/**/*"
  ],
  "scripts": {
    "prepublishOnly": "rimraf dist && npm run build",
    "build": "tsup src/index.ts --dts --format esm --minify --out-dir lib",
    "dev": "npm run build -- --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "esbuild": "^0.11.12"
  },
  "devDependencies": {
    "@nbhr/utils": "^0.0.16",
    "@types/node": "*",
    "rimraf": "*",
    "svelte": "^3.37.0",
    "ts-standard": "*",
    "tsup": "*",
    "typescript": "*"
  },
  "engines": {
    "node": ">13.x"
  }
}