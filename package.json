{
  "author": "",
  "bugs": {
    "url": "https://github.com/alexanderniebuhr/sveasy/issues"
  },
  "dependencies": {
    "esbuild": "^0.11.11"
  },
  "description": "",
  "devDependencies": {
    "@nbhr/utils": "0.0.16",
    "@types/node": "*",
    "rimraf": "*",
    "svelte": "^3.37.0",
    "ts-standard": "*",
    "tsup": "*",
    "typescript": "*"
  },
  "files": [
    "lib/**/*"
  ],
  "homepage": "https://github.com/alexanderniebuhr/sveasy#readme",
  "license": "ISC",
  "main": "lib/index.js",
  "bin": "bin/sveasy.js",
  "name": "sveasy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexanderniebuhr/sveasy.git"
  },
  "scripts": {
    "prepublishOnly": "rimraf dist && npm run build",
    "build": "tsup src/index.ts --dts --format esm,cjs --minify --out-dir lib",
    "dev": "npm run build -- --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "version": "0.2.0",
  "engines": {
    "node": ">13.x"
  }
}
